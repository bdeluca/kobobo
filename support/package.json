{
  "name": "kobobo-support",
  "version": "1.0.0",
  "description": "Playwright MCP setup for Kobobo testing",
  "type": "module",
  "scripts": {
    "install-browsers": "playwright install",
    "test": "playwright test",
    "start-mcp": "node playwright-mcp-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.6.0",
    "@playwright/test": "^1.48.0"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}